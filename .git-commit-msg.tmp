F5 调试页 + F6 概览页：命令流水/求值诊断/掉项审计/原始帧检查器 + 可折叠六面板

F5 的存在理由是仓库 commit 史里那一整类「op 发了但没生效」的 bug（do(True) 静默忽略、
挂件订单出现但实体不出现、建造确认超时、换位重试）—— 曾为此写了 4 个一次性探针脚本。
应用/落地在 B9（D6/D7）之前显示「未知」而不是留空，否则会让人以为一切正常。
这里也是唯一允许显示 burnysc2 原生名的地方（ability_raw 是诊断字段，不是类型身份）。

F6 按 ADR-0023 §2.1 组织但不做可拖拽 dock（决策 U9），折叠状态存 localStorage，
每块可点「完整页 →」跳转。

测试：后端 506 / 前端 63
