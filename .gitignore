# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# uv / venv
.venv/

# 工具缓存
.ruff_cache/
.idea/
.vscode/

# 运行产物（run_*.py 生成；trace fixture 缺则自动 skip，重新生成见 run_recorder.py）
*.log
docs/evidence/state_trace.jsonl

# OS
Thumbs.db
.DS_Store
.zcode/
# 运行时数据（提案日志、录制的帧、agent 工作区与 trace）—— 不进版本库
runtime/
traces/

# 密钥（用户提供的 BaseAgent .env，复制进来供本地测试；绝不入库）
.env